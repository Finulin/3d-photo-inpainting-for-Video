numpy>=1.24
opencv-python>=4.7.0
vispy==0.6.4
moviepy==1.0.2
transforms3d==0.3.1
networkx==2.3
scikit-image

# Progress bar utility used by stereo_from_depth_video.py
tqdm>=4.65

# Note: 'cynetworkx' provides a C-accelerated version of networkx but requires
# a build step (Cython) and may fail to install via pip on some systems.
# If you do NOT need it, you can skip installing 'cynetworkx' and use 'networkx'
# which is pure-Python and already listed above. To install cynetworkx safely,
# prefer conda-forge or pre-install Cython and build tools (see README).
# cynetworkx
